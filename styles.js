import { StyleSheet } from 'react-native';

// Example usage:
const styles = StyleSheet.create({
  container: {
    padding: 16,
  },
  item: {
    padding: 10,
    marginVertical: 8,
    backgroundColor: '#f9c2ff',
  },
});
